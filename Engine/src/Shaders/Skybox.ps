// VARIANTS BEGIN
// VARIANTS END

TextureCube skyboxTexture : register(t0);
SamplerState skyboxTextureSampler : register(s0);

float4 main(float3 texCoord : TEXCOORD) : SV_TARGET
{
    return float4(skyboxTexture.Sample(skyboxTextureSampler,texCoord).rgb, 1.0f);
}